#include <stdio.h>
 /**
  * main - Prints the alphabet in lowercase
  *
  * Description: 'the programs description'
  *
  * Return: Always (0)
  */

int main(void)
{
	char c;

	for (c = 'a'; c <= 'z'; c++)
		putchar(c);
	putchar('\n');
	return (0);
}
